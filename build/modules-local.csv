# Local-only modules — built by build-all.sh but not by build-all.yml CI.
# Same format: name,dep1,dep2,...  (build order matters)
# All modules moved to modules-core.csv or modules-applications.csv — everything
# should be in the CI publication pipeline.
